# flint_x86_64
New Device flint_x86_64

Found in device/pebble/flint_x86_64

This repo is added to the flint manifest in default.xml as:

<project path="device/pebble/flint_x86_64" name="flint_x86_64" remote="github" />
